[build]
builder = "NIXPACKS"

[deploy]
startCommand = "PYTHONPATH=/app/backend:$PYTHONPATH uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
 healthCheckPath = "/api/health"
 healthCheckTimeout = 100

[deploy.env]
PYTHONPATH = "/app/backend"
